import React from "react";

/**
 * Upgrade prompts are no longer used; all features are included.
 * Component kept for backwards compatibility — renders nothing.
 */
function UpgradePrompt() {
  return null;
}

export default UpgradePrompt;

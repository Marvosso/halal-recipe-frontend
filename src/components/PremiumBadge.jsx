import React from "react";

/**
 * Premium badges are no longer used; all features are included.
 * Component kept for backwards compatibility — renders nothing.
 */
function PremiumBadge() {
  return null;
}

export default PremiumBadge;
